package com.petstore.petservice.domain.model.enums;

public enum EntityType {
    PET,           // For images only
    BREED,         // For reviews only
    PET_PRODUCT    // For both images and reviews
}
